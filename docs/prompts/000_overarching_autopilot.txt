    # Milestone 000 — Optional autopilot (run all milestones sequentially)

    You are operating in an autonomous Codex run (`--full-auto`) inside this repository.

Follow `AGENTS.md` exactly:
- Read and update `CONTINUITY.md` at the start and end.
- Use `uv` for deps, Ruff for format/lint, pytest for tests.
- Do not commit or expose raw TriNetX data (treat `data/` as sensitive and git-ignored).
- Keep the computational core pure; isolate I/O and orchestration.
- Prefer correctness → clarity → reproducibility → measured performance.

Global guardrails:
- Do NOT run on real `data/` unless explicitly required by this milestone; tests must not depend on it.
- Do NOT delete legacy notebooks; archive/move only when asked.
- Do NOT introduce new linters/formatters besides Ruff.
- When you make behavior-affecting decisions, record them in `docs/DECISIONS.md`.

Output expectations for this run:
- Start with a Ledger Snapshot (Goal + Now/Next + Open Questions) sourced from `CONTINUITY.md`.
- End with: commands run, files changed, test results, and the exact next prompt filename to run.


    Ticket: TRIN-000
    Objective: Execute milestones 001–014 in order in a single run, only if safe.

    Important:
    - This is OPTIONAL. The recommended workflow is to run each milestone prompt individually.
    - If you encounter ambiguity, missing dependencies, failing tests, or large unreviewable diffs,
      STOP and report. Do not plow forward.

    Plan:
    1. Read `docs/prompts/INDEX.md` to get the canonical sequence.
    2. For each prompt file (001..014):
       - Execute its tasks.
       - Run `ruff format`, `ruff check`, `pytest -q` (and any extra verification required).
       - Update `CONTINUITY.md`.
       - Commit with the suggested message (unless repo policy forbids auto-commits).
       - Continue only if tests pass and working tree is clean.
    3. If any step requires user input (paths, data), stop and write precise instructions.

    Definition of done:
    - All milestones completed; `pytest -q` passes at end; docs updated; repo is runnable via CLI.

    If at any point this becomes unsafe, stop and recommend switching to per-milestone execution.
