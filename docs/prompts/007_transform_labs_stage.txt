    # Milestone 007 — Lab-results stage extraction

    You are operating in an autonomous Codex run (`--full-auto`) inside this repository.

Follow `AGENTS.md` exactly:
- Read and update `CONTINUITY.md` at the start and end.
- Use `uv` for deps, Ruff for format/lint, pytest for tests.
- Do not commit or expose raw TriNetX data (treat `data/` as sensitive and git-ignored).
- Keep the computational core pure; isolate I/O and orchestration.
- Prefer correctness → clarity → reproducibility → measured performance.

Global guardrails:
- Do NOT run on real `data/` unless explicitly required by this milestone; tests must not depend on it.
- Do NOT delete legacy notebooks; archive/move only when asked.
- Do NOT introduce new linters/formatters besides Ruff.
- When you make behavior-affecting decisions, record them in `docs/DECISIONS.md`.

Output expectations for this run:
- Start with a Ledger Snapshot (Goal + Now/Next + Open Questions) sourced from `CONTINUITY.md`.
- End with: commands run, files changed, test results, and the exact next prompt filename to run.


    Ticket: TRIN-007
    Objective: Extract lab-results logic into pure transforms and a stage runner; add tests and stage CLI.

    Scope
    - Implement lab-results processing only.
- Preserve semantics from the canonical lab notebook(s).
- Use synthetic fixtures; do not depend on `data/`.


    Tasks
    1. Read/update `CONTINUITY.md`.
2. Identify canonical legacy lab processing notebook(s); document in `docs/DECISIONS.md`.
3. Implement pure transforms:
   - Module: `src/trinetx_preprocessing/transform/labs.py`
   - Extract/derive fields exactly as legacy code (codes, units, etc.).
   - Avoid per-row python loops.
4. Stage runner:
   - Module: `src/trinetx_preprocessing/pipeline/labs_stage.py`
   - Function: `run_labs_stage(cfg) -> list[Path]`.
5. CLI:
   - Add `run-labs --config path`.
6. Tests:
   - Synthetic lab records covering inclusion/exclusion, code mapping, missing values.
   - Integration test running `run-labs` using temp dirs.
7. Docs updates:
   - Data contract: required lab columns and identifiers.
   - Validation: what tests now guarantee.
8. Update `CONTINUITY.md` at end.


    Verification
    ```bash
uv sync
ruff format
ruff check
pytest -q
python -m trinetx_preprocessing run-labs --help
```


    Definition of done
    - Labs transform is pure and tested.
- Labs stage runner processes synthetic inputs end-to-end.
- CLI supports `run-labs`.


    Suggested commit message
    - feat: extract labs stage into pure transform + runner

    Next prompt to run
    - docs/prompts/008_transform_diagnosis_stage.txt
